/* eslint-env mocha */

import assert from 'assert';

describe('delete_me', () => {
  it('passes the tests but should be deleted when you start working on this', () => {
    assert(true);
  });

  it('works with async and await', async () => {
    await 1;
    assert(true);
  });
});
